{{!< default}}

<section class="{{page_class}}">
  {{> "heros/home"}}
</section>

<section id="home" class="xc-page">
    <div class="xc-page__content">

      <h3>HIIII</h3>

      {{#page}}
        {{content}}
      {{/page}}

      <div class="xc-postfeed">
        {{#get "posts" limit="6"}}
          {{#foreach posts}}

              {{> "cards/post"}} {{!-- partials/card.hbs --}}

          {{/foreach}}
        {{/get}}
      </div>

    </div>
</section>

{{> "shop/shopify"}}